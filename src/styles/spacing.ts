/**
 * Spacing schema
 *
 * Used for elements spacing, UI sizes, roundness, etc.
 */
enum Spacing {
  zero = 0,
  tiny = 4,
  base = 8,
  medium = 12,
  double = 16,
  large = 24,
}

export default Spacing;
